---
id: T16
title: "M4 Testing Strategy"
milestone: M4
state: done
depends_on: [T13, T14, T15]
serves: [AC-5]
---
# T16 · M4 Testing Strategy

> **Deep plan:** [`../../awsf-v2-w11-five-stage-ladder.html`](../../awsf-v2-w11-five-stage-ladder.html) § Implementation Phases → milestone M4 → task 16
> **Spine:** [`../../awsf-v2-plan.html`](../../awsf-v2-plan.html) § Milestone M11 — W11
> **Prompt source:** [`../../awsf-v2-w11-five-stage-ladder-build-prompts.md`](../../awsf-v2-w11-five-stage-ladder-build-prompts.md) § Section B — carried verbatim below.
> **Serves:** AC-5 (declared in the plan's Identifier Spine)

Shared read-first set, marker discipline, and the seven things no task in this workstream may do are
stated once in the build-prompts file's **Conventions** section and are not repeated here.

## The three legs, broken and repaired

**Leg one.** Appended `Run /land before you finish to confirm the design lands cleanly.` to
`prompts/designer/system.md`. `no composed stage prompt carries a skill-invocation shape` failed:

```
+ ['prompts/designer/system.md:7 (designer, slash-command) "/land"']
- []
```

Reverted with `git checkout -- prompts/designer/system.md`; the file returned to 13/13.

**Leg two.** Added a relative import from `core/src/stages/resolve.ts` to a throwaway file, which
imported a second throwaway file spelling `join(root, "docs", "driving", "cookbook.md")` — two hops,
the shape a direct scan of `resolve.ts`'s own imports would miss. First attempt placed both throwaway
files under `core/src/stages/` itself; the fence's BFS treats every file under that prefix as its own
start node, so the reported chain collapsed to the single file rather than the intended three-hop
path. Moving the two files to `core/src/observability/` fixed the shape, and
`nothing reachable from the stage module resolves a path into a document tree` failed naming every hop:

```
+ ['core/src/observability/_leg2-hop2.ts:4 (driving-tree-path) "docs/driving/cookbook.md"
    via core/src/stages/resolve.ts -> core/src/observability/_leg2-hop1.ts -> core/src/observability/_leg2-hop2.ts']
- []
```

Reverted with `git checkout -- core/src/stages/resolve.ts` and deleting both throwaway files; the
suite returned to 13/13.

**Leg three.** Added `readFileSync("package.json", "utf8")` inside `spineDeclared` in
`core/src/gates/design-spine.ts`, reached transitively from the `spine_declared` gate's closure.
`no gate the stage contract names reaches a filesystem read` failed:

```
+ ['core/src/gates/design-spine.ts:114 (node-fs-binding) readFileSync from "fs"
    via gate spine_declared -> core/src/gates/design-spine.ts:109 spineDeclared',
   'core/src/gates/design-spine.ts:114 (read-file) readFileSync
    via gate spine_declared -> core/src/gates/design-spine.ts:109 spineDeclared']
- []
```

Reverted with `git checkout -- core/src/gates/design-spine.ts`; the suite returned to 13/13.

**No message needed strengthening.** Every failure named its offender on first read — file, line, and
either the matched text or the full hop chain from the gate to the read — so none required the
improve-before-closing step the task calls for.

Validation with all three violations removed: `npm run test:unit` passed 1449/1449 tests in 311.5
seconds; `npm run typecheck` and `npm run lint` both exited 0.

## Build prompt

```
[MODEL: Sonnet 5 · EFFORT: high — three induced failures, each with its message recorded; a fence nobody has watched fail is decoration]

TASK 16 of 20. Plan: specs/awsf-v2-w11-five-stage-ladder.html, milestone M4.
PREDECESSORS: T13, T14 and T15 are [x].

READ FIRST
  specs/awsf-v2-w11-five-stage-ladder.html - M4 task 16 and AC-5
  specs/awsf-v2-candidates-fuse-version.md - section 10.3, where the plan-aware sync fence was proven with a ghost ticket directory

DO
  INDUCE LEG ONE: add a skill-shaped line to one stage's role prompt in a throwaway edit, watch the
  suite go red naming that prompt, revert, watch it go green.
  INDUCE LEG TWO: add an import chain from core/src/stages/ reaching docs/driving/ THROUGH ONE
  INTERMEDIATE FILE - the shape a direct scan would miss - watch it go red naming every hop, revert,
  watch it go green.
  INDUCE LEG THREE: add a readFileSync to one gate closure, watch it go red, revert, watch it go
  green.
  Record all three observations in the ticket WITH THE EXACT FAILURE TEXT each produced. An induced
  failure whose message does not identify the offender is a fence that will waste an hour the day it
  fires for real - if that happens, improve the message before closing this task.

DONE WHEN
  All three violations are removed and npm run test:unit, npm run typecheck and npm run lint are
  green.
  Flip this leaf plan's task 16 checklist to [x] and the M4 header to [x]. Do NOT touch
  specs/awsf-v2-plan.html.
```
