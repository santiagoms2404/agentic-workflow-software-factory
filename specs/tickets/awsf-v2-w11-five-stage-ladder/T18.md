---
id: T18
title: "The envelope join at each boundary"
milestone: M5
state: failed
depends_on: [T17]
serves: [AC-6, INV-2]
---
# T18 · The envelope join at each boundary

> **Deep plan:** [`../../awsf-v2-w11-five-stage-ladder.html`](../../awsf-v2-w11-five-stage-ladder.html) § Implementation Phases → milestone M5 → task 18
> **Spine:** [`../../awsf-v2-plan.html`](../../awsf-v2-plan.html) § Milestone M11 — W11
> **Prompt source:** [`../../awsf-v2-w11-five-stage-ladder-build-prompts.md`](../../awsf-v2-w11-five-stage-ladder-build-prompts.md) § Section B — carried verbatim below.
> **Serves:** AC-6, INV-2 (declared in the plan's Identifier Spine)

Shared read-first set, marker discipline, and the seven things no task in this workstream may do are
stated once in the build-prompts file's **Conventions** section and are not repeated here.

## Build prompt

```
[MODEL: Opus 5 · EFFORT: high — comparing by shape rather than by bytes is the difference between a join test and a snapshot of a scripted adapter]

TASK 18 of 20. Plan: specs/awsf-v2-w11-five-stage-ladder.html, milestone M5.
PREDECESSORS: T17 is [x].

READ FIRST
  specs/awsf-v2-w11-five-stage-ladder.html - M5 task 18, AC-6, INV-2
  core/src/observability/queries.ts - envelopesForPhase
  core/test/fixtures/stages/S1.json through S5.json - the captured shapes

DO
  At each boundary, read the stored envelope from the projection through envelopesForPhase and
  assert its schema id equals the one that stage's record in the contract names.
  Compare against the captured fixture BY SHAPE, NOT BYTES: the schema id and the presence of every
  required field. The captured bytes are one run's output; asserting them would make the journey a
  snapshot test of a scripted adapter, which proves nothing about the join.
  Assert the later stage's input is derived from the earlier stage's STORED ENVELOPE, and that
  nothing in the chain reconstructs a value from a file the earlier stage happened to write.
  Record the route log at the end and assert it contains no provider launch beyond the scripted
  stub, using the RouteLog idiom rather than a count.

DONE WHEN
  npm run test:journeys is green.
  Flip this leaf plan's task 18 checklist to [x]. Do NOT touch specs/awsf-v2-plan.html.

ADDENDUM — read before acting on the prompt above. The prompt predates T04's
capture finding and contradicts the plan's own T18 markers. The plan wins.

C1  Three of the four DO items are [f] in specs/awsf-v2-w11-five-stage-ladder.html
    under block W05-HOST-COMMAND-ENVELOPES: the schema-id read, the shape
    comparison, and the derived-input assertion. S1, S2 and S5 store no typed
    envelope at all, and the S1->S2 and S2->S3 boundaries have no earlier stored
    envelope. Leave all three [f] with the block named. Leave T18.md at
    state: failed. Do NOT flip the whole checklist to [x].

C2  Do not close the gap by adding a schema. INV-2 says ENVELOPE_SCHEMAS gains
    nothing from this workstream; INV-6 says it adds no mechanism. The routing is
    already decided and already written: the plan's Notes section holds the T04
    amendment request to W05 naming the three ids (awsf.init-output/v1,
    awsf.project-register-output/v1, awsf.publish-output/v1). Nothing to re-derive.

C3  The one live row is already implemented. core/test/journeys/five-stage.test.ts
    lines 411-412 record the route log and assert stub-only contact, because T17's
    third bullet needed the same evidence. Verify it, do not duplicate it. Caveat
    worth a judgment call: the assertion is a deepEqual against a five-element
    literal, which names each contact but does encode the count. Tighten to a set
    comparison if you read the row strictly.

C4  The harness moved. world() and the offline machinery now live in
    core/test/unit/cli/_offline-route.ts, imported by both design-to-plan-route.test.ts
    and the journey. The prompt's READ FIRST list points at the old location.

C5  Do not "fix" the file-existence assertions at lines 330, 374 and 385. Those
    observe each stage's entry precondition. They are not the chain reconstructing
    a value from a file, which is what the third [f] row bans.

Realistic scope: verify one row, flip one checklist item, one commit.
envelopesForPhase exists at core/src/observability/queries.ts:365 and is not
needed for any unblocked work.
```
