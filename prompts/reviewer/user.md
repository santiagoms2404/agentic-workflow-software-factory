Review the exact candidate represented by the prior host evidence. Accept only when no blocking defect remains; every concern must cite concrete repository evidence.

Previous phase envelope:
{previous_envelope}

Return only an envelope satisfying this host-generated contract:
{output_schema}
