[self-compact · notice] Heads-up only. Context is at {{used_tokens}} tokens ({{used_percent}}) of {{context_window}}. The warning line is at {{warning_tokens}} and the hard cutoff at {{forced_tokens}}, {{remaining_to_forced}} tokens away. Nothing is blocked; keep working. You choose when to compact, and the best moments are the natural boundaries of driving: after a report to the owner, after a run is launched and you are waiting on it, or after an owner decision is carried out. Call `view_context` when you need current numbers. Compaction cycles so far: {{cycle}}.
